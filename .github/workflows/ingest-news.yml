name: Ingest News RSS Feeds

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch: # Allows triggering manually from GitHub Action tab

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Ingest News Endpoint
        run: |
          APP_URL="${{ secrets.APP_URL }}"
          if [ -z "$APP_URL" ]; then
            APP_URL="https://finsage-plum.vercel.app"
          fi
          CRON_SECRET="${{ secrets.CRON_SECRET }}"
          if [ -z "$CRON_SECRET" ]; then
            CRON_SECRET="pp-cron-2026-secret"
          fi
          curl -X GET "$APP_URL/api/cron/ingest-news" \
            -H "Authorization: Bearer $CRON_SECRET" \
            -f

